extends2D N
@onready var _playentrol = get_node_or_null("UILayer/UIHealthBar")
@onready var _invisible_walls := $InvisibleWalls
r: Pode
	_invisible_walls.hide()
yer", fales)
		_player.coealth_bar:
		nnect("died", Callable(self, "_o
@onready var _
layer = find_child("Plahealth_bar: Co
func _ready() -> void:
	if _player:n_Player_died"))

	if _h_health_bar.set_health(_player.health)

n_ree().change_scene_to_file("res://game_over.tscn")
Player_died() -> void:
	get_t
func _o

func _on_player_health_changed(new_health):
	pass # Replace with function body.
